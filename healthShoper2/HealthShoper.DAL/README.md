# 🧱 DataAccess Layer (DAL)

> Слой доступа к данным отвечает за взаимодействие с базой данных, настройку моделей и их конфигураций.  
> Его основная цель — изолировать работу с БД от остальной логики приложения, обеспечивая чистую архитектуру и удобство сопровождения.

---

## 📁 Структура проекта

```text
DataAccess/
├── Context/
│   ├── AppDbContext.cs
│   └── Configurations/
│       ├── ClientConfiguration.cs
│       ├── ItemConfiguration.cs
│       └── BasketConfiguration.cs
│
├── Interfaces/
│   └── IApplicationDbContext.cs
│
├── Models/
│   ├── Client.cs
│   ├── Item.cs
│   └── Basket.cs
│
└── Seeds/
    └── DataSeed.cs
```

---

## ⚙️ 1. Context

Содержит классы, обеспечивающие подключение и работу с базой данных.

- **`AppDbContext`**  
  Основной контекст приложения, определяющий наборы данных (таблицы) и подключение к БД.  
  Работает через интерфейс, обеспечивая слабую связанность.

- **Папка `Configurations`**  
  Хранит конфигурации моделей для Entity Framework.  
  Определяет:
   - связи между сущностями,
   - ограничения и ключи,
   - типы и размеры полей.

---

## 🧩 2. Interfaces

Определяет интерфейсы для безопасного и управляемого доступа к БД.

- **`IApplicationDbContext`**  
  Интерфейс, который скрывает детали реализации `AppDbContext`.  
  Используется в слоях приложения (например, в Application), чтобы:
   - не зависеть напрямую от EF Core,
   - ограничить доступ только нужной логикой,
   - упростить тестирование и внедрение зависимостей.

---

## 🧠 3. Models

Описывает модели данных, которые соответствуют таблицам в БД.

- **`Client`** — модель пользователя, отвечает за регистрацию, авторизацию и хранение основных данных.
- **`Item`** — модель товара, который доступен для покупки.
- **`Basket`** — модель корзины, связывающая пользователя и выбранные им товары.

---

## 🌱 4. Seeds

Содержит начальные данные для базы данных.

- **`DataSeed`**  
  Выполняет инициализацию базы данных начальными значениями (тестовые пользователи, товары и т.д.).  
  Используется при первом запуске или миграции, чтобы система могла корректно стартовать с базовыми данными.

---

## ✅ Основные задачи DAL

- Организация доступа к данным через EF Core
- Централизация конфигураций моделей
- Изоляция слоя данных от бизнес-логики
- Упрощение тестирования и сопровождения приложения

---

## 💡 Рекомендации

- Избегайте прямого обращения к `AppDbContext` — используйте `IApplicationDbContext`
- Все связи и ограничения определяйте в `Configurations`
- При добавлении новых моделей не забывайте обновлять `DataSeed`, если требуются начальные данные
- Поддерживайте миграции в актуальном состоянии (`dotnet ef migrations add ...` / `dotnet ef database update`)

---

📚 **DataAccess** — фундамент для всей работы с данными в вашем приложении.
